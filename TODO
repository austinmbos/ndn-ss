edge computing device


1. try pyNDN app in docker container without NFD

run tests for performance of docker containers

finish all microservices

once microservices api is up and running, build application

application is going to be anonymous communication using encrypted proxy in ndn
